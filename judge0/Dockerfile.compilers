# DEPRECATED / SUPERSEDED
# The compilers setup is now handled automatically via a multi-stage Docker build 
# inside the main judge0/Dockerfile.
# Run `docker compose up --build` from the root directory to build the stack directly.
