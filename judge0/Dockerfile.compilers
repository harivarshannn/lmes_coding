# LEGACY BASE COMPILERS IMAGE PLACEHOLDER
# Note: The compilers setup is now handled automatically via a multi-stage Docker build
# inside the main judge0/Dockerfile. You can run the stack directly using:
# docker compose up --build -d

FROM ubuntu:22.04
RUN echo "Compilers setup is now handled via multi-stage builds. This image is a legacy placeholder."
